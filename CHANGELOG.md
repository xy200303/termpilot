# 更新日志

## 0.2.0

助手接不上时，可以用本机命令操作同一个终端，不用再要口令。

- 新增 `tools`、`schema`、`call`。Windows 在 `%APPDATA%\TermPilot\bin\termpilot.cmd`。
- 命令和窗口里的助手共用同一份工具。
- 修正各助手实际读取的配置文件。Cursor 和 CodeBuddy 可以一键写入。

安装包没有代码签名。

## 0.1.0

你在窗口里登录远程电脑，助手接着用这个还没断开的会话。密码和私钥口令留在本机。做到一半要输入时，按键打进当前画面，不用每做一步重新连接。

- 没有公网地址的机器可以连回来，只接受本机连入。
- 删除和危险命令先在窗口里等你点允许。
- 可以按文件夹改远程文件，也可以截窗口里实际显示的画面。

安装包没有代码签名。
