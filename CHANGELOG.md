# 更新日志

## 0.5.5

经过跳板时，文件树挂在已经连到目标机的那条 SSH 会话上，不再为了传文件再向跳板要一次转发。

关掉终端或文件树其中一边，另一边还在；两边都关，连接才断。

安装包没有代码签名。

## 0.5.4

跳板用户名会保留冒号后面的整段。有的平台把令牌写在用户名里，拆成密码会被拒绝转发。平台不让直接转发时，会在跳板里再登录目标机，并用已保存的目标机密码应答。

粘贴命令时会认超时、保活、主机密钥、压缩、多跳和 ProxyCommand，连接时按这些选项拨号。

新建连接分成连接、认证、跳板、高级四个选项卡。

安装包没有代码签名。

## 0.5.3

可以粘贴带 `ssh -J` 的命令。先登录跳板，再从跳板连到目标机。跳板口令和目标机密码分开存在本机。

安装包没有代码签名。

## 0.5.2

滚动截图超过大约 20 屏时，结果里会写明缓冲一共多少行、实际截到哪一段。以前只留下末尾，看起来像已经截完。

启动时如果窗口停在白屏，会重新把第一帧画出来。

安装包没有代码签名。

## 0.5.1

合成截图的字边不再带彩色描边。长图放大后，笔画保持原来的颜色。

安装包没有代码签名。

## 0.5.0

设置里的「合成算法」打开后，当前画面、滚动缓冲、选中的行，以及助手发起的截图，都按缓冲里的字符和颜色拼成图片，不滚动正在看的画面。拼不出来会停下来，并提示关掉。

- 关掉之后仍是逐屏拍摄。
- 截图没有完成时会弹出说明。

安装包没有代码签名。

## 0.4.0

离开屏幕渲染改到界面后面，用另一块 WebGL 终端画完再读它的画面。画不出来会停下来，并提示关掉这个开关。

- 不再盖住正在看的终端。关掉这个开关后，仍是逐屏拍摄。
- 截图时，盖在终端上的设置窗口和菜单不会被拍进去。
- 设置里可以看当前版本，并对照 GitHub 发布页检查有没有更新。有新版本时打开下载页，不会自动下载。

安装包没有代码签名。

## 0.3.0

本机命令换成安装包里的小程序，不用再装 Node。窗口关着时，这条命令会先把 TermPilot 打开。

- Windows、macOS、Linux 都会把 `termpilot` 放进当前用户的 PATH。卸掉 Windows 版时会把这一项去掉。
- 中文路径和带引号的参数可以直接写在命令后面。
- 登录目录下的绝对路径如果打不开，会再按相对路径试一次。
- 设置里的「长截图」默认关着。打开后，超过一屏的范围不再滚动正在看的画面。

安装包没有代码签名。

## 0.2.0

助手接不上时，可以用本机命令操作同一个终端，不用再要口令。

- 新增 `tools`、`schema`、`call`。Windows 在 `%APPDATA%\TermPilot\bin\termpilot.cmd`。
- 命令和窗口里的助手共用同一份工具。
- 修正各助手实际读取的配置文件。Cursor 和 CodeBuddy 可以一键写入。

安装包没有代码签名。

## 0.1.0

你在窗口里登录远程电脑，助手接着用这个还没断开的会话。密码和私钥口令留在本机。做到一半要输入时，按键打进当前画面，不用每做一步重新连接。

- 没有公网地址的机器可以连回来，只接受本机连入。
- 删除和危险命令先在窗口里等你点允许。
- 可以按文件夹改远程文件，也可以截窗口里实际显示的画面。

安装包没有代码签名。
