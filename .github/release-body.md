连接和终端各自有固定编号。连接是 `conn-` 开头，终端是 `term-` 开头。名称和备注只用来认出它们是干什么的，调用时只填编号。

同一个地址下面可以放多条连接。地址这一行只做分组，右边显示账号；不是 22 端口时带上端口，走跳板时标出「经跳板」。备注停在上面能看到。

终端可以改名、写备注。退出软件再打开，同一扇终端的编号、标题、备注和最近输出还在。主动关掉，或删掉那条连接，就不会再出现。

助手的工具改成 `connection_*` 和 `term_*`。已经装过的 skill 需要按 README 里的安装说明重装一次。

安装包没有代码签名。
