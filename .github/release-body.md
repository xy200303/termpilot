- 增加了与 `~/.ssh/config` 的同步：启动时读入里面写明的主机，在软件里改主机、端口、用户、私钥或跳板后写回同一份配置。密码和备注仍只留在本机。
- 修复了文件通道：外部 sftp-server 文件在、但仍以 127 退出时，改为 sshd 自带的 internal-sftp，并用新连接重试。
- 修复了文件通道打不开时，错误信息下面还显示一个 `.` 目录的问题。

安装包没有代码签名。
